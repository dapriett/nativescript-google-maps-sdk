<Page xmlns="http://www.nativescript.org/tns.xsd" xmlns:maps="nativescript-google-maps-sdk" loaded="pageLoaded">
    <GridLayout>
        <maps:mapView latitude="{{ latitude }}" longitude="{{ longitude }}"
                      zoom="{{ zoom }}" bearing="{{ bearing }}"
                      tilt="{{ tilt }}" i-padding="50,50,50,50" padding="{{ padding }}" mapReady="onMapReady" 
                      markerSelect="onMarkerEvent" markerBeginDragging="onMarkerEvent"
                      markerEndDragging="onMarkerEvent" markerDrag="onMarkerEvent"
                      markerInfoWindowTapped="onMarkerEvent" coordinateTapped="onCoordinateTapped"
                      cameraChanged="onCameraChanged">
            <maps:mapView.infoWindowTemplate>
                <StackLayout orientation="vertical" width="200" height="220" >
                    <Image src="https://unsplash.it/200/?random" stretch="none"  height="200" width="200" className="infoWindowImage" />
                    <Label text="{{position.latitude + ',' + position.longitude}}" className="infoWindowCoordinates"  />
                </StackLayout>
            </maps:mapView.infoWindowTemplate>
            <maps:mapView.infoWindowTemplates>
                <template key="testWindow">
                    <StackLayout orientation="vertical" width="200" height="125" >
                        <Image src="https://www.google.com/images/errors/logo_sm_2.png" stretch="none"  height="50" width="150" className="infoWindowImage" />
                        <Label text="Current Location" cssClass="title"  />
                        <Label text="{{position.latitude + ',' + position.longitude}}" className="infoWindowCoordinates"  />
                    </StackLayout>
                </template>
            </maps:mapView.infoWindowTemplates>
        </maps:mapView>
    </GridLayout>
</Page>